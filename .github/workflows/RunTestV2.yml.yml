name: BuildWrokflowV2

on:
  push:
    branches: [ dev_v2 ]
  pull_request:
    branches: [ master_v2 ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Clean
      run: make clean
    - name: Run unit tests
      run: make all